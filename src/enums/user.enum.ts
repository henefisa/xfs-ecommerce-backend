export enum EUserRole {
  SUPER_ADMIN = 'super admin',
  ADMIN = 'admin',
  USER = 'user',
  GUEST = 'guest',
  SELLER = 'seller',
}

export enum EUserStatus {
  ACTIVE = 'active',
  DEACTIVE = 'deactive',
  BANNED = 'banned',
}
